const APP_VERSION = '2.3.9.2';
const APP_NAME = 'Gestión de Territorios';

// Note: This script is intended to be run in a browser console if possible, 
// or I will try to sync it via Firestore using a temporary trigger in app.js
console.log(`Bumping to ${APP_VERSION}`);
